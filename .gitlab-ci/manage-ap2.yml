# =============================================================================
# manage-ap2 / tgw
# =============================================================================
manage-ap2-tgw:validate:
  extends: .terraform_base
  stage: validate
  variables:
    TF_ROOT: account/aws-manage-ap2/tgw
  script:
    - terraform validate
  rules:
    - if: $CI_PIPELINE_SOURCE == "push"
      changes:
        - account/aws-manage-ap2/tgw/**/*
    - when: manual
      allow_failure: true

manage-ap2-tgw:plan:
  extends: .terraform_base
  stage: plan
  variables:
    TF_ROOT: account/aws-manage-ap2/tgw
  script:
    - terraform plan -out=tfplan
  artifacts:
    paths:
      - ${TF_ROOT}/tfplan
    expire_in: 1 hour
  rules:
    - if: $CI_PIPELINE_SOURCE == "push"
      changes:
        - account/aws-manage-ap2/tgw/**/*
    - when: manual
      allow_failure: true

manage-ap2-tgw:apply:
  extends: .terraform_base
  stage: apply
  resource_group: manage-ap2-tgw
  variables:
    TF_ROOT: account/aws-manage-ap2/tgw
  script:
    - terraform apply -auto-approve tfplan
  dependencies:
    - manage-ap2-tgw:plan
  rules:
    - when: manual
      allow_failure: true

# =============================================================================
# manage-ap2 / vpc
# =============================================================================
manage-ap2-vpc:validate:
  extends: .terraform_base
  stage: validate
  variables:
    TF_ROOT: account/aws-manage-ap2/vpc
  script:
    - terraform validate
  rules:
    - if: $CI_PIPELINE_SOURCE == "push"
      changes:
        - account/aws-manage-ap2/vpc/**/*
    - when: manual
      allow_failure: true

manage-ap2-vpc:plan:
  extends: .terraform_base
  stage: plan
  variables:
    TF_ROOT: account/aws-manage-ap2/vpc
  script:
    - terraform plan -out=tfplan
  artifacts:
    paths:
      - ${TF_ROOT}/tfplan
    expire_in: 1 hour
  rules:
    - if: $CI_PIPELINE_SOURCE == "push"
      changes:
        - account/aws-manage-ap2/vpc/**/*
    - when: manual
      allow_failure: true

manage-ap2-vpc:apply:
  extends: .terraform_base
  stage: apply
  resource_group: manage-ap2-vpc
  variables:
    TF_ROOT: account/aws-manage-ap2/vpc
  script:
    - terraform apply -auto-approve tfplan
  dependencies:
    - manage-ap2-vpc:plan
  rules:
    - when: manual
      allow_failure: true
